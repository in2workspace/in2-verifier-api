spring:
  application:
    name: verifier-api

api:
  externalDomain: http://localhost:8080
  clientId: "did:web:dome-marketplace-dev.org"
  paths:
    authenticationResponsePath: /api/v1/authorization-response
  cacheStore:
    lifetime: 10
    timeUnit: MINUTES
