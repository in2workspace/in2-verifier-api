server:
  port: 9000

logging:
  level:
    es.in2: info

spring:
  application:
    name: VC Verifier
  web:
    resources:
      static-locations:
        - classpath:/static/
        - classpath:/resources/

security:
  authorizationServer:
  token:
    accessToken:
      cronUnit: "DAYS"
      expiration: 30
    refreshToken:
      cronUnit: "DAYS"
      expiration: 10

crypto:
  privateKey: